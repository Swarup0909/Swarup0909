# Morya Lodging

Simple static site for Morya Lodging.

## About
This repository contains the project's static files (HTML, images).

## How to push
Run the following to add and push this README (replace `main` with your branch if different):

```
git add README.md
git commit -m "Add README"
git push origin main
```

If you need me to create a more detailed README, tell me what to include (installation, usage, license, etc.).
